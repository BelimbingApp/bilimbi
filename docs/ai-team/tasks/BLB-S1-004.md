# BLB-S1-004 — Core Company department support

**Stage:** S1
**Status:** Active — grandfathered external work
**Owner:** parent-thread/codex
**Role:** Module implementer
**Base Commit:** `54b50b0`
**Dependencies:** Core Company schema contract; supports BLB-S1-003
**Write Claim:** `apps/core/company/**`
**Shared Paths:** None claimed

## Context

This work existed in the shared worktree before the coordination steward
bootstrap. The reservation records reality; it does not redefine or take
ownership of the parent task.

Observed dirty paths at steward bootstrap (2026-08-12):

- `apps/core/company/lib/company.ex` (adds
  `department_belongs_to_company?/3` public API);
- `apps/core/company/lib/company/schema_contract.ex` (moves
  `company_departments_head_id_foreign` into the required foreign-key map);
- `apps/core/company/lib/company/department.ex` (new `Department` schema
  over `company_departments`).

The change supports the in-flight Core Employee foundation (BLB-S1-003):
Employee records reference tenant-owned company departments. The broad
module claim covers later files created by the same parent task.

## Coordination rule

No other agent may edit, format, stage, revert, or broaden changes under
`apps/core/company/` until the owner hands off or the user cancels that
work. Shared compatibility, root documentation, lockfile, or ModuleRegistry
changes are not included and require a separate integration claim.

## Acceptance criteria

Owned by the pre-existing parent task. At minimum, the later handoff should
state the department contract, its Belimbing schema compatibility (including
the `head_id` self-reference to `employees`), tenant-company scoping, public
API surface, and focused tests.

## Work log

- 2026-08-12: Registered as a grandfathered reservation by the amp/kimi-k3
  steward bootstrap; accepted by steward claude/opus-5 during handover, who
  matched the board row to this card.
- 2026-08-12: `schema_contract.ex` returned to clean (its
  `company_departments_head_id_foreign` edit moved to the Employee
  baseline). The observed-paths list above remains the historical bootstrap
  record; the live dirty paths are `company.ex` and `department.ex`.
- 2026-08-12: Added the private Department schema and the tenant/company-aware
  `department_belongs_to_company?/3` collaboration API with focused coverage.
  Core Company suite: 13 passed.

## Handoff

Ready for independent review. Employee never reaches into Company's private
queries or schema: it validates department ownership through the new public
boolean API, which first proves that the company belongs to the tenant and is
live. The canonical department `head_id` foreign key remains physically owned
by Employee's later migration. Company's schema contract should mark that
foreign key required only during shared integration, after Compatibility
declares Employee as a dependency.
