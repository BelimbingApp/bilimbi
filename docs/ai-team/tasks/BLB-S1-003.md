# BLB-S1-003 — Core Employee foundation

**Stage:** S1
**Status:** Review — rework complete on Scope API; awaiting re-review
**Owner:** cursor/grok-4.5 (user-authorized takeover from parent-thread/codex)
**Role:** Module implementer
**Base Commit:** rebased onto `origin/main` (`0c65647`) after PR #1 Scope rewrite
**Dependencies:** Core Company and the canonical Employee source contract
**Write Claim:** `apps/core/employee/**`
**Shared Paths:** None claimed

## Context

This module directory appeared in the shared worktree while the message board
was being created. The reservation records concurrent parent-task ownership;
it does not approve, redesign, or take over that implementation.

## Coordination rule

No other agent may edit, format, stage, revert, or broaden changes under
`apps/core/employee/` until the owner hands off or the user cancels that work.
Shared compatibility, root documentation, lockfile, or ModuleRegistry changes
are not included and require a separate integration claim.

## Acceptance criteria

Owned by the pre-existing parent task. Its eventual handoff should establish
the canonical schema contract, migration order, Company/department/supervisor
integrity, Employee type policy, addressable identity, public API, system-agent
bootstrap semantics, tests, and explicit User/AI deferrals.

Review rework (codex/gpt-5, 2026-08-12) additionally required:

- protect the durable platform-orchestrator identity through public create,
  update, and delete paths;
- reject reserved-code collisions during system-type bootstrap instead of
  silently converting adopted custom rows;
- preserve Belimbing's `employment_start` on orchestrator creation.

## Work log

- 2026-08-12: Grandfathered reservation; Core Employee package completed by
  parent-thread/codex; focused suite 7 passed; handed off for review.
- 2026-08-12: Independent review by codex/gpt-5 returned `changes required`.
- 2026-08-12T21:44+08: User-authorized takeover by `cursor/grok-4.5` after
  Codex rate limit.
- 2026-08-12T21:55+08: Reworked against `origin/main` Scope APIs (PR #1):
  reserved `SYS-001` protection, system-type bootstrap preflight,
  `employment_start` on provision, and public APIs take `Tenancy.Scope`.
  Focused module suite: **11 passed**. Format check: pass.

## Handoff

Ready for independent re-review against
`docs/ai-team/reviews/BLB-S1-003--codex-gpt-5.md`.

Platform orchestrator contract:

- durable identity `(platform-operator primary company, SYS-001, agent)`;
- public create/update/delete cannot reserve, rewrite, or remove it;
- resolve/ensure fail closed on collisions and primary-company transfer that
  leaves the agent behind;
- provision sets `employment_start` to the creation date.

Shared compatibility registration remains on BLB-S1-009.
