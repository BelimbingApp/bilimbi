# BLB-S1-003 — Core Employee foundation

**Stage:** S1
**Status:** Active — grandfathered external work
**Owner:** parent-thread/codex
**Role:** Module implementer
**Base Commit:** `54b50b0`
**Dependencies:** Core Company and the canonical Employee source contract
**Write Claim:** `apps/core/employee/**`
**Shared Paths:** None claimed

## Context

This module directory appeared in the shared worktree while the message board
was being created. The reservation records concurrent parent-task ownership;
it does not approve, redesign, or take over that implementation.

Observed files at board bootstrap:

- `apps/core/employee/.formatter.exs`;
- `apps/core/employee/bilimbi.module.exs`;
- `apps/core/employee/mix.exs`;
- `apps/core/employee/docs/README.md`.

The broad module claim covers later files created by the same parent task.

## Coordination rule

No other agent may edit, format, stage, revert, or broaden changes under
`apps/core/employee/` until the owner hands off or the user cancels that work.
Shared compatibility, root documentation, lockfile, or ModuleRegistry changes
are not included and require a separate integration claim.

## Acceptance criteria

Owned by the pre-existing parent task. Its eventual handoff should establish
the canonical schema contract, migration order, Company/department/supervisor
integrity, Employee type policy, addressable identity, public API, system-agent
bootstrap semantics, tests, and explicit User/AI deferrals.

## Work log

- 2026-08-12: Concurrent directory detected and registered as a grandfathered
  reservation during board bootstrap.
- 2026-08-12: At 19:34+08 the module briefly moved to a new
  `apps/people/` Domain container; the owner reverted the relocation and
  `apps/core/employee/` again holds a coherent Core descriptor. Claim
  narrowed back to `apps/core/employee/**` by steward amp/kimi-k3. The
  episode and its layer analysis are recorded on `BLB-S1-005`.
- 2026-08-12: Completed the Core Employee package, compatible migration and
  schema contract, tenant/company-scoped public API, employee types,
  platform-orchestrator identity, and focused tests. Focused module suite: 7
  passed.

## Handoff

Ready for independent review. The module owns the canonical `employees` and
`employee_types` structures and the depending-module foreign key
`company_departments.head_id -> employees.id`. Employee CRUD validates the
tenant-owned company and rejects cross-company department, supervisor, and
custom-type references behind public read models.

System employee types bootstrap idempotently. The platform orchestrator is
resolved and provisioned by the explicit platform-operator company plus
employee number `SYS-001`; numeric Employee ID 1 has no runtime meaning. A
conflicting non-agent record fails as an invariant violation. User linkage,
authentication, AI activation/execution, authorization, and Web workflows are
explicitly deferred to their owning later slices.

Shared compatibility registration, fresh migration replay, and root docs are
pending the requested integration claim.
