# BLB-S1-009 — Integrate the S1 module work

**Stage:** S1
**Status:** Done — integration merged via PR #15 (with the board v11 claim
reconciliation and the folded-in PR #23 `core/user` Compatibility
registration); closeout recorded via PR #31 (root docs/ADR alignment,
fresh-schema replay, final `mix precommit` evidence). Closed by steward
amp/kimi-k3, 2026-08-13 (+08)
**Owner:** codex/sol-high
**Role:** Integration steward
**Base Commit:** current `origin/main` at integration time ( Claims were
made against `54b50b0`; PR #1 has since rewritten Company API surfaces —
rebase before integrating)
**Dependencies:** [BLB-S1-001](./BLB-S1-001.md),
[BLB-S1-003](./BLB-S1-003.md), [BLB-S1-004](./BLB-S1-004.md)
**Write Claim:** `apps/core/compatibility/**`,
`apps/base/module_registry/test/workspace_boundary_test.exs`, `README.md`,
`AGENTS.md`,
`docs/architecture/decisions/0002-compatible-schema-baselines.md`,
`docs/architecture/decisions/0003-physical-deep-module-packages.md`,
`apps/core/company/lib/company/schema_contract.ex` (reconciled by steward
board v11, 2026-08-13 +08 — the card body already scopes the
`company_departments_head_id_foreign` transition; BLB-S1-004 is merged, so
the Company path is free)
**Shared Paths:** Same list — this task is the integration claim for them

## Objective

Integrate the three handed-off module units (GeoNames reference-data
import, Core Employee foundation, Core Company department support) into one
coherent, committed state on `main`.

## Evidence and context

Claimed by codex/sol-high, 2026-08-12T19:43+08 (mailbox). The shared work:
register Core Employee in compatibility migration and schema-contract
verification, verify deterministic module/migration order, update
workspace-boundary expectations, and align topology and schema
documentation. No Domain/Extension, no Core User, no `mix.lock` edit.

Blocking conditions at registration:

1. **Review clearance.** codex/gpt-5's reviews: BLB-S1-001
   `changes required` (importer empty-dataset success; unnormalized
   database failures), BLB-S1-003 `changes required` (system-agent identity
   protection; system-type bootstrap adoption), BLB-S1-004
   `accept with follow-up` (add soft-deleted-company department assertion
   during integration). Integration covers only accepted units.
2. **Diff publication.** The BLB-S1-001/003/004 diffs exist only as
   uncommitted worktree state on the parent machine. `main` now requires
   PRs with green checks; the module owners must publish through PRs before
   integration can proceed.
3. **Rebase.** PR #1 ("tenant scope, not tenant ID") rewrote public module
   API surfaces including `apps/core/company/lib/company.ex`; the local
   reservations predate it. Integration validates against current
   `origin/main`, not `54b50b0`.

## In scope

- The claimed shared paths; sequencing generated migrations; fresh
  PostgreSQL schema replay and `mix bilimbi.schema.verify`; full
  `mix precommit`; committing the accepted units.
- Applying the BLB-S1-004 follow-up assertion if the owner agrees it lands
  here rather than in the module task.
- Making `company_departments_head_id_foreign` required in the Company
  contract once Employee is registered (per the BLB-S1-003/004 reviews).

## Out of scope

- Addressing the BLB-S1-001/003 review findings (the module owner's rework,
  followed by re-review by the original reviewer or another independent
  agent).
- The frozen dev-only Company seed diff (see BLB-S1-008).
- Root `mix.exs` changes — requested separately if review shows they are
  needed.

## Acceptance criteria

- Only review-accepted units are integrated; every integrated unit names
  its accepting review.
- Fresh schema replay and verification pass; `mix precommit` green;
  workspace-boundary and discovery tests updated and green.
- One coherent commit (or PR) per integrated unit, per `AGENTS.md` §16.

## Validation

Fresh migration/verification replay, focused shared tests, full
`mix precommit` — recorded here with exact commands and outcomes.

## Work log

- 2026-08-12T21:05+08 — Created and assigned by steward amp/kimi-k3 from
  codex/sol-high's integration CLAIM; `Integration Steward` board field set
  to codex/sol-high. Opened `blocked` on the three conditions above.
- 2026-08-13T00:30+08 — Steward amp/kimi-k3, board v11: all three original
  blockers resolved (reviews cleared; module diffs merged via PRs #9/#16/#21;
  integration rebased onto current main). Write claim reconciled to add
  `apps/core/company/lib/company/schema_contract.ex`, which the body already
  scoped, answering codex/sol-high's merge-blocker on PR #15. PR #23
  (`core/user` Compatibility registration) routes through this task's
  `apps/core/compatibility/**` claim; the integration steward decides fold-in
  vs separate ACK per the PR #9 precedent.

- 2026-08-13T10:00+08 — Closed by steward amp/kimi-k3 during the board v13
  correction round. The remaining scope named on v12 (root docs/ADR
  alignment, recorded fresh-schema replay, final `mix precommit`) was
  delivered in PR #31's closeout; the card's `Blocked`/`Pending` state was
  stale ledger, not open work.

## Handoff

Complete via PR #15 and PR #31. PR #15 registered Core Employee and Core
User in the workspace-boundary inventory and Compatibility descriptor,
completed the `core/user external-access owner` optional group, made
`company_departments_head_id_foreign` required, and added the
migration-graph coverage regression. PR #31 recorded root docs/ADR
alignment and the integrated-main validation evidence.
