# BLB-S1-007 — SchemaVerifier column-type vocabulary

**Stage:** S1
**Status:** Active
**Owner:** claude/opus-5
**Role:** Module implementer (Base Database)
**Base Commit:** `163734d`
**Dependencies:** None
**Write Claim:** `apps/base/database/**`
**Shared Paths:** None (`mix.lock` untouched)

## Objective

Extend `Bilimbi.Base.Database.SchemaVerifier.type_matches?/2` so schema
contracts can express the four PostgreSQL column types Belimbing's
Base/Core schema requires: `:uuid`, `{:char, n}`, `:jsonb`, `:inet`.

## Evidence and context

`type_matches?/2` currently has eleven clauses
(`{:varchar, n}`, `{:timestamp, n}`, `{:numeric, p, s}`, `:bigint`,
`:boolean`, `:date`, `:double_precision`, `:integer`, `:json`, `:smallint`,
`:text`) and no catch-all, so a contract naming an unknown type raises
`FunctionClauseError` instead of reporting drift. Belimbing usage,
confirmed by the implementer against Laravel `PostgresGrammar.php`
(`laravel/framework ^13.14.0`):

- `uuid` — `notifications.id` (`app/Core/User`, `0200_01_20_000005:23`);
  Core User critical path;
- `char(n)` — `user_pins.url_hash` is `char(32)` (`0200_01_20_000003`);
  Core User critical path;
- `jsonb`, `inet` (`ipAddress()`) — required by Base Audit in S2; added now
  to avoid reopening this file.

Scope decision: the implementer recommended a separate task ((b)); the
user chose the direct claim ((a)) for speed. Recorded in claude/opus-5's
mailbox, 2026-08-12T20:52+08. The implementer explicitly flagged that this
concentrates shared foundation and its first consumer in one agent, so this
small task deserves a careful independent review.

## In scope

- The four type clauses plus focused tests, including drift reporting for
  each new type.
- A stated decision on the missing catch-all clause (raise vs. report
  drift), covered by tests either way.

## Out of scope

- Core User itself (BLB-S1-006), the production-seed ledger
  (BLB-S1-008 — waits for this task's handoff on the same path claim),
  refactoring unrelated verifier code.

## Acceptance criteria

- Contracts can declare all four types and verification reports drift
  rather than raising on both matching and mismatching columns.
- Focused tests for each new clause; `mix format --check-formatted` clean.
- No `mix precommit` (integration gate; product reservations are active).

## Validation

To be recorded by the implementer.

## Work log

- 2026-08-12T21:05+08 — Card created and claim ACKed by steward amp/kimi-k3,
  registering the user-decided scope from claude/opus-5's SCOPE/DECISION
  messages. Blocks BLB-S1-006; blocks BLB-S1-008 by path overlap.

## Handoff

Pending.
