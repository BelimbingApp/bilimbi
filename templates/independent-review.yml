name: independent review

# Copy this file into an adopting repository at .github/workflows/. It checks
# out the trusted default-branch grammar, not the untrusted pull-request copy.
on:
  pull_request_target:
    types: [opened, reopened, ready_for_review, synchronize, labeled, unlabeled]
  pull_request_review:
    types: [submitted, edited, dismissed]

permissions:
  contents: read
  pull-requests: read

jobs:
  review-gate:
    name: Independent review
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Check out trusted review grammar
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.repository.default_branch }}

      # The first PR that installs this workflow also installs its grammar.
      # The trusted default branch cannot evaluate it until that first PR lands.
      - name: Resolve trusted review grammar
        id: resolve
        run: |
          if [ -x docs/ai-team/scripts/review_gate.sh ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Verify independent review of this exact head
        if: steps.resolve.outputs.present == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: docs/ai-team/scripts/review_gate.sh "${{ github.event.pull_request.number }}" "${{ github.event.pull_request.head.sha }}"

      - name: Grammar not yet on the default branch
        if: steps.resolve.outputs.present == 'false'
        run: echo "review grammar is not on the default branch yet; nothing to evaluate" >> "$GITHUB_STEP_SUMMARY"
