[
  id: "base/principal_directory",
  kind: :module,
  layer: :base,
  required: true,
  otp_app: :bilimbi_base_principal_directory,
  namespace: Bilimbi.Base.PrincipalDirectory,
  dependencies: ["base/module_registry", "base/tenancy"],
  migrations: nil,
  web: nil,
  schema_contract: nil,
  contribution_provider: nil
]
