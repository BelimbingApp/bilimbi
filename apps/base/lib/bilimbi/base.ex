defmodule Bilimbi.Base do
  @moduledoc """
  Platform infrastructure and shared contracts for Bilimbi.

  Base owns mechanisms used by business Domains without owning their business
  meaning. Higher layers depend on Base; Base does not depend on them.
  """
end
